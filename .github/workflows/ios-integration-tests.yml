name: Run iOS Instrumentation Tests on macos-14

on:
  push:
    branches:
      - ios-work-flow
    paths:
      - '.github/workflows/**'


jobs:
  test:
    runs-on: macos-14
    permissions:
      contents: read
      pull-requests: write
    
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Xcode Version
        run: |
          sudo xcode-select -s /Applications/Xcode_15.4.app/Contents/Developer
          xcodebuild -version

      - name: Setup CMake
        uses: jwlawson/actions-setup-cmake@v2
        with:
          cmake-version: 'latest'

      - name: Verify CMake Installation
        run: cmake --version

      - name: Setup Ruby and Bundler
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.0'
          bundler-cache: false

      - name: Install CocoaPods
        run: |
          gem install cocoapods
          pod --version

      - name: Cache CocoaPods
        uses: actions/cache@v4
        with:
          path: |
            sdks/ios/example/Pods
            ~/.cocoapods/repos
          key: ${{ runner.os }}-pods-${{ hashFiles('sdks/ios/example/Podfile.lock') }}
          restore-keys: |
            ${{ runner.os }}-pods-

      - name: Install Third-Party Binaries
        run: |
          cd $(git rev-parse --show-toplevel)
          chmod +x ./setup.sh
          ./setup.sh --sdk ios

      - name: Build NimbleNet.xcframework
        run: |
          cd $(git rev-parse --show-toplevel)
          chmod +x ./sdks/ios/script/build-deliteAI-static.sh
          ./sdks/ios/script/build-deliteAI-static.sh

      - name: Install CocoaPods Dependencies
        working-directory: sdks/ios/example
        run: |
          pod install --repo-update

      - name: List Available Simulators
        run: |
          xcrun simctl list devices available

      - name: Build for Testing
        working-directory: sdks/ios/example
        run: |
          xcodebuild build-for-testing \
            -workspace NimbleNetExample.xcworkspace \
            -scheme NimbleNetExample \
            -destination 'platform=iOS Simulator,name=iPhone 15,OS=17.5' \
            -configuration Debug \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO

      - name: Run Unit Tests
        working-directory: sdks/ios/example
        run: |
          xcodebuild test-without-building \
            -workspace NimbleNetExample.xcworkspace \
            -scheme NimbleNetExample \
            -destination 'platform=iOS Simulator,name=iPhone 15,OS=17.5' \
            -configuration Debug \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO

      - name: Upload Test Results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: test-results
          path: |
            sdks/ios/example/build/Logs/Test/
            sdks/ios/example/TestResults/
          retention-days: 30
